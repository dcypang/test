/* ============================================================
   Deployment settings.

   Leave this file alone and the game runs on open map tiles with no key.

   To power the map round with Google Maps for everyone who plays your
   copy, paste a Google Maps JavaScript API key below. See the README for
   what the key needs (a Cloud project with billing, the Maps JavaScript
   API enabled, and an HTTP-referrer restriction naming your domain).

   A Maps JavaScript key is always visible in the page — that is how the
   API works, and it is why the referrer restriction, not secrecy, is what
   stops other sites spending your quota. Never put a key here that is not
   restricted, and never reuse a server-side key.
   ============================================================ */
window.GEO_CONFIG = {
  googleMapsKey: ''
};
